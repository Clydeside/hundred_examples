#include <iostream>
using namespace std;

// Calculating sum of two numbers
int main() {
    int first = 0, second = 0, sum = 0;
    cout << "Enter first number: ";
    cin >> first;
    cout << first << "\nEnter second number: ";
    cin >> second;
    cout << "\n The sum of two entered values: " << first + second << "\n";
    return 0;
}